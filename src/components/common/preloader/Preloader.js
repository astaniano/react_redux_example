import preloader from "../../../assets/images/45.gif";

const Preloader = (props) => {
    return (<div style={{backgroundColor: "white"}}>
        <img src={preloader} alt={"no img"}/>
    </div>)
}

export default Preloader;